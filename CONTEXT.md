# TCP Chat Client

基于单线程 poll() 事件循环的纯 C 语言 TCP 聊天客户端（学习项目），配套 TCPServer（`d:\AAA_Game_XueXiBan\ShareUbuntu\CC\TCPServer`）。提供登录、大厅聊天功能；私聊、群组、文件传输在后续里程碑规划中。

## Language

**连接 (Connection)**:
客户端到服务器的 TCP 连接，由 `conn_t` 封装；连接是客户端唯一的 I/O 通道。建立后该通道即可读（服务器下发帧）可写（客户端发送帧）。
_Avoid_: Socket、套接字、链路

**会话 (Session)**:
客户端一侧的会话状态——登录后的用户名、读缓冲、事件循环开关。单线程内只有一个会话，与服务器的多会话不同。
_Avoid_: 上下文、Client

**登录 (Login)**:
客户端以用户名+密码发送 MSG_AUTH 帧的行为；与服务器术语"认证"对应同一协议动作，但客户端是用户视角，称登录。_行为规则_: 服务器对登录失败不关闭连接——收到错误帧后应在**同一连接上重新提示输入并重试**，直到成功或用户中断（Ctrl-D）。
_Avoid_: 认证（认证是服务器侧说法，见 TCPServer CONTEXT.md）

**帧 (Frame)**:
协议的最小传输单元：Magic(0xCAFE) + 消息类型 + 载荷长度(2B 大端) + 载荷；与 TCPServer CONTEXT.md 定义一致。_行为规则_: 解析是增量的——半帧合法，`protocol_parse` 返回 0 表示需要更多数据；返回 -1（magic 不符）是致命错误，客户端直接断开，不尝试重新同步。
_Avoid_: 报文、数据包

**命令 (Command)**:
以 `/` 开头的用户输入行（/login、/help、/quit），由 commands 模块分发。_行为规则_: 不以 `/` 开头的输入行是裸文本，作为大厅消息（MSG_CHAT）发送，不进入命令分发。
_Avoid_: 指令、斜杠命令

**提示符 (Prompt)**:
ui 层输出的 `> ` 前缀，标志 stdin 等待输入；收到服务器消息时 ui 负责在消息前后重排提示符（避免消息打断用户正在输入的行）。
_Avoid_: 光标、输入行

**读缓冲 (Read buffer)**:
app 持有的可增长字节缓冲，承载 recv 数据与增量帧解析之间的暂存。_行为规则_: 64KiB 起步、翻倍扩容、256KiB 硬顶；每次 recv 前必须保证缓冲有空余，超限按协议错误断开。
_Avoid_: 缓存、缓冲池

**事件循环 (Event loop)**:
poll 驱动的"读流→增量解析→分发→写回"循环；单次循环同时观察 socket 可读与 stdin 可读，200ms 超时用于观察 SIGINT 标志。循环是客户端所有行为的驱动源。
_Avoid_: 主循环、主线程

**分发 (Dispatch)**:
按帧类型把完整帧路由到对应处理函数的动作。_行为规则_: 每个收到的完整帧恰好分发一次；未知消息类型走默认分支提示，不阻塞循环。M2-M4 的新帧类型在分发 switch 上扩展。
_Avoid_: 路由（路由是服务器侧说法）

**广播回显 (Echo)**:
服务器把大厅聊天广播给包括发送者在内的所有已认证会话；客户端发送后自己也会收到 `发送者: 消息` 格式的回显。_行为规则_: 回显是预期行为，不是 bug；客户端原样打印，不自行去重。
_Avoid_: 重复消息、发送确认

**服务器公告 (Announcement)**:
以 `[Server] ` 前缀出现的 MSG_SERVER_MSG 文本（欢迎、上线/下线公告、离线消息提示等）。服务器已格式化，客户端原样打印。文件传输的 tid 只存在于公告文本中（M4 涉及）。
_Avoid_: 系统消息、通知

**私聊 (Private message)**:
`/priv <用户名> <消息>` 命令触发的点对点消息，载荷为 `target\0message`（MSG_PRIV）。_行为规则_: 服务器格式化后以三种形态回发，客户端全部原样打印——目标收到 `发送者 (private): 消息`，发送方收到 `To 目标: 消息` 回声，离线回放为 `发送者 (offline): 消息`；目标不存在时收到错误帧（ERR_NO_SUCH_USER）。与大厅广播不同，私聊**无**发送者本人的 `(private)` 形态回显——回声是 `To 目标:` 前缀。
_Avoid_: 点对点消息、悄悄话

**离线消息 (Offline message)**:
目标不在线时服务器缓存的私聊（ADR-0002：内存存储、每用户上限、FIFO）。_行为规则_: 登录后服务器按帧序回放——先 `[Server] You have N offline message(s)` 公告，再逐条以 MSG_PRIV 离线形态发送；回放顺序=原接收顺序，且发生在在线列表帧之前。
_Avoid_: 留言、离线私信

**在线列表 (User list)**:
`/users` 命令发送零载荷 MSG_USERS 请求，服务器回 MSG_ONLINE_USERS 帧（载荷: count(2B 大端) + \0\0 预留 + user1\0user2...，用户名从偏移 4 开始切分）。_行为规则_: 登录成功时服务器会自动下发一次，/users 用于手动刷新；客户端只显示不缓存。
_Avoid_: 用户名单、成员列表

**群组 (Group)**:
`/gcreate` `/gjoin` `/gleave` `/gmsg` 命令作用的命名聊天空间，载荷为群名（单文本）或群名\0消息（MSG_GMSG）。_行为规则_: 群由服务器持久（进程内）持有，最后一名成员离开时自动销毁；创建/加入/离开的反馈是 MSG_SERVER_MSG 公告或 MSG_ERROR 错误帧，客户端原样显示；断连时服务器静默移除成员身份（无公告）。
_Avoid_: 房间、频道

**群聊消息 (Group message)**:
MSG_GMSG 帧承载的群内广播，服务器格式化 `[群名] 发送者: 消息`。_行为规则_: 与大厅广播同构——含发送者本人的回显，是预期行为；仅群成员可发言，非成员发言收错误帧。客户端原样打印，不区分处理。
_Avoid_: 群广播、组消息

**群组成员 (Group member)**:
加入群组后获得该群发言权与会话关联身份的用户。_行为规则_: 创建者自动成为首个成员（无特权，与普通成员同权）；重复加入被服务器拒绝（ERR_ALREADY_MEMBER）。
_Avoid_: 群友、群用户

**文件传输 (File transfer)**:
`/sendfile` 命令触发的点对点文件发送会话，由 MSG_FILE_* 帧族承载。_行为规则_: 服务器分配传输标识并中转全部控制帧与数据块；目标必须在线（不做离线排队）；进行中任一方可取消（/cancel）；任一方断线即取消（服务器只发文本公告，不发取消帧）。
_Avoid_: 传输会话、传送

**传输标识 (Transfer id)**:
服务器为每个文件传输分配的全局唯一编号（tid，单调递增）。_行为规则_: **只出现在服务器公告文本中**（"[Server] File transfer #N ..." / "Incoming file transfer #N ..."），控制帧与数据块载荷里只有二进制 tid 字段；客户端从公告文本解析后用于 /accept /reject /cancel。
_Avoid_: 传输编号、会话号

**分片 (Chunk)**:
文件按固定大小切成的数据块，MSG_FILE_CHUNK 载荷为 `tid(4B) + offset(8B) + data`。_行为规则_: 发送方按 offset 顺序发送；接收方**按 offset 定位写盘（fseek），乱序到达是正常现象**（服务器线程池并行转发，实测相邻块交换/循环错位）——只有重复/越界 offset 才视为错误（触发主动取消 + 删半成品）；单分片数据受服务器读缓冲限制（≤65518 字节）。_Avoid_: 数据块、包、片段

**接收目录 (Download directory)**:
接收方落盘文件的目录（客户端运行目录下 downloads/）。_行为规则_: 文件名先做 basename 化（防目录穿越，服务器不校验文件名）再唯一化（同名加序号）；取消/失败时删除半成品与不完整文件。
_Avoid_: 下载文件夹、收件箱

## 词汇表维护

新增模块（M2-M4）时同步扩展本表：每个新概念配定义 + `_Avoid_`，核心概念附行为规则。本表是当前对话与未来会话/子代理的共享语言。
