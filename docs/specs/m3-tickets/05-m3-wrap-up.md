# 05 — M3 收尾：文档 + 全场景回归

**What to build:** 群组里程碑的完整交付：项目文档（里程碑状态、功能清单）更新到 M3 完成；用真实服务器 + 多客户端跑通全场景 e2e——创建 → 加入 → 双向群聊（含回显）→ 离开 → 最后一人删群，以及全部错误路径（重名/重复加入/群不存在/非成员），确认无回归。

**Blocked by:** 02 — /gcreate 创建群；03 — /gjoin + /gleave 加入/离开群；04 — /gmsg 群聊 + MSG_GMSG 分发

**Status:** ready-for-agent

- [ ] CLAUDE.md「当前开发状态」更新：M3 完成、检查点、待办指向 M4 文件传输
- [ ] README 功能特性与命令说明更新到 M3
- [ ] 全场景 e2e 冒烟通过：创建→加入→双向群聊（含发送者回显）→离开→最后一人删群
- [ ] 错误路径 e2e 通过：重名 203、重复加入 202、群不存在/非成员 201
- [ ] Release 与 Debug 预设构建 + ctest 全绿（test_protocol + test_commands 全部用例）
